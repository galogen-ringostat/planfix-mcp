# Roadmap — friction-driven development

Guiding rule (Anthropic, [Writing effective tools for agents](https://www.anthropic.com/engineering/writing-tools-for-agents)): grow this server by removing observed agent friction, not by covering API endpoints. Every item below traces to a real stumble in production sessions. New items enter this file only with a named friction source.

Development loop: friction observed in a live session → item recorded here → KB session writes a WHAT-brief → developer session implements → KB session reviews the diff and runs a Layer 3 live check (docs/TESTING.md) → push.

## P1 — composite & search tools (observed friction)

1. **`get_task_full`** — return task fields + comments in a single call. **Implemented 2026-07-24** (`src/tools/tasks.ts`), **Layer 3 verified 2026-07-24** (live read-only run: all filters discriminate correctly against production data).
   Friction: syncing one card of the vault task mirror currently costs `get_task` + `get_comments` (2 round-trips, 2× token overhead). Follow the consolidation guidance: one tool per real workflow step.
2. **`search_tasks`** — filtered task search (name substring, assignee, status, project, updated-since). **Implemented 2026-07-24** (`src/tools/tasks.ts`), **Layer 3 verified 2026-07-24** (live read-only run: all filters discriminate correctly against production data).
   Friction: the only discovery path today is paging `get_tasks`. Planfix REST supports complex task filters (see [REST API: Complex task filters](https://planfix.com/help/REST_API:_Complex_task_filters)). Search-shaped beats list-shaped.

## P2 — time entries (investigation first)

3. **Investigate, then implement, writing time entries** (analytics/datatags on tasks).
   Friction: time logging is fully manual today; a 2026-07 attempt to write time entries via MCP failed — root cause never diagnosed (tool gap vs API gap). Step 1 is a spike: determine whether Planfix REST can create the "time spent" analytics records at all (safe-mode ON, MCP-Test project only). Only if yes, design the tool. Highest-value item in the file if the API allows it.
   **Spike done 2026-07-24 — verdict: TOOL GAP.** The REST API writes time entries fine (data tag 59 "Time spent" via `POST /task/{id}/datatags/`; verified round-trip on MCP-Test task 572467, entry key 127822). Full evidence, request/response shapes, and the proposed `add_time_entry` tool design: `docs/spikes/time-entries.md`.
   **Implemented 2026-07-24** (`src/tools/timeentries.ts`: `add_time_entry` + `get_task_time_entries`, incl. the review-mandated `commentId` chaining for the one-comment-per-period convention), **Layer 3 verified 2026-07-24**: both endpoint variants live-tested on task 572467 — new-comment (keys 127823/127825) and append after the 6425999 fix (keys 127824/127826); one-comment-per-period convention round-trips (entries share commentId 47856607).

## P3 — hygiene per Anthropic checklist (background, batch with other work)

4. **Tool annotations**: `readOnlyHint` / `destructiveHint` / `idempotentHint` on all 20 tools (currently absent). **Done 2026-07-24** — all 24 tools migrated to `registerTool` with annotations; descriptions and `.describe()` strings also moved to English (test-enforced), **Layer 3 read-only sweep verified 2026-07-24** (24 annotated tools, zero Cyrillic in schemas; probe-path has_more exact on a full 100-row get_tasks page; list_custom_fields client paging exact incl. offset numbering; CONCISE cuts get_task output ~3.5x on live data).
5. **Pagination metadata**: list tools return `has_more` + next offset hint instead of a bare page. **Done 2026-07-24** — exact `has_more` via `src/paging.ts` (over-fetch, or a one-row probe at the API's 100 cap); only schema addition: `list_custom_fields` gained `offset`/`pageSize` (client-side paging — its GET endpoint has none).
6. **`response_format` parameter** (`CONCISE` | `DETAILED`) on the heaviest read tools (`get_task`, `get_tasks`, `get_contacts`) to cut token cost when the agent only needs identifiers. **Done 2026-07-24** — also on `get_task_full`; default DETAILED, backward compatible.

## P4 — log_workday (approved next, 2026-07-24)

7. **`log_workday`** — composite day-level time logging on top of `add_time_entry`. **Implemented 2026-07-24** (`src/tools/timeentries.ts`; shared `writeTimeEntry()` core with `add_time_entry`), **Layer 3 verified 2026-07-24**: real day in MCP-Test — validate_only plan matched the real run exactly; lunch-spanning 12:30-16:45 auto-split to 12:30-14:00 + 15:00-16:45 (keys 127827/127828, one commentId 47856609); second task chained separately (key 127829); overlap probe refused with zero writes.
   Takes a whole working day (list of per-task intervals) and **deterministically enforces the operator's logging conventions before writing anything**: intervals must not overlap across tasks; no interval may cross the 14:00–15:00 lunch break (an interval spanning it is split into two entries); entries for the same task+period chain onto one comment via `commentId`. Rationale: these rules currently live only as instructions to the consuming agent (operator's vault, planfix page § Working conventions) — deterministic code beats discipline. Approved by the operator 2026-07-24 as the next slice; detailed brief to follow from the reviewing session.
   **v2 2026-07-24 (operator decision — no hardcoded conventions):** the lunch rule was removed from code and generalized to a **required `exclusions` input** (N no-work windows `{timeFrom, timeTo, label?}`, merged when overlapping/adjacent, cut out of every interval with per-window `(auto-split: label)` marks; `[]` = log straight through; no default of any kind). Semantics change: partial window overlap now splits instead of refusing. **Layer 3 v2 verified 2026-07-24**: live day — omitted exclusions refused at schema level; adjacent windows merged (standup+sync → 11:00-12:00); both intervals cut correctly (keys 127830-127833, per-task chaining intact); validate_only plan matched the real run exactly.

## P5 — get_task_children (approved 2026-07-24)

8. **`get_task_children`** — direct subtask listing for a given parent task (read-only). **Implemented 2026-07-24** (`src/tools/tasks.ts`; task/list complex filter type 73 "direct parent task" — no dedicated endpoint exists; type 307 = recursive subtree, deliberately not used), **Layer 3 verified 2026-07-24**: real hierarchy — parent 401356 (autofill Deal branch) returned exactly its 9 direct Ф0-Ф8 subtasks (parent itself absent from the list); a leaf subtask and an unrelated task both return the empty hint. Direct-only semantics consistent with live data + the 73-vs-307 docs distinction (no 3-level hierarchy existed to probe grandchild exclusion directly).
   Friction: the operator's time entries live in subtasks (e.g. autofill hours in Ф3/Ф4 branches) and the vault task mirror cannot see task hierarchy today — finding subtasks means UI navigation. Promoted from the researched backlog by operator decision 2026-07-24.
   Note (observed while checking the swagger for a children endpoint, 2026-07-24): the REST v2 swagger now lists `/task/{id}/checklist`, `/task/{id}/checklist/list`, `/task/{id}/checklist/{itemId}` — this contradicts the "checklists not exposed" out-of-scope entry below; worth a re-verification spike before dismissing checklists again.

## P6 — checklist re-verification spike (approved 2026-07-24)

9. **Checklist spike** — re-verify the "checklists are not exposed" verdict (2026-07-20) against the `/task/{id}/checklist*` endpoints discovered in the current swagger during P5. Friction: checklists are the single biggest manual gap — the operator pastes them by hand into every task-context conversation. Spike first, tools only after design review (P2 precedent).
   **Spike done 2026-07-24 — verdict: FULLY EXPOSED; the 2026-07-20 verdict is obsolete.** List/get/create/update (incl. `isDone` toggle) all work: reads verified on 11 production tasks (401356, 425626, all Ф0–Ф8 children), write round-trip verified on MCP-Test task 572468 (item 572469 created and checked). Likely root cause of the old verdict: tool-surface confusion (the upstream npx server had no checklist tool; REST was never probed directly) — same misdiagnosis shape as P2. Full evidence, schemas, and the proposed tool design (`get_task_checklist`, `add_checklist_item`, `set_checklist_item_done` with safe-mode wiring): `docs/spikes/checklists.md`.
   **Implemented 2026-07-24** (`src/tools/checklists.ts`, design approved as proposed; 29 tools total), **Layer 3 verified 2026-07-24**: item 572470 created with assigneeId 403 (assignee round-trips — spike open question closed), checked, un-checked, listed with correct [x]/[ ] states on probe task 572468. failures-array branch: a bogus-field probe returned HTTP 400 fail-envelope (handled by client.ts), not 200-with-failures — the defensive branch stays live-unexercised.

## P7 — update_checklist_item_name (approved 2026-07-24)

10. **`update_checklist_item_name`** — rename a checklist item. Friction: operator asked for it directly 2026-07-24 (rewriting items is part of maintaining checklists; deletion is impossible — the API answers 405, verified live — so rename + un-check is also the "cancel this item" workaround). The update endpoint accepts `name` (rename round-trip verified live on MCP-Test item 572470 by the reviewing session).
   **Implemented 2026-07-24** (`src/tools/checklists.ts`; 30 tools total; description documents the "[cancelled]" prefix + un-check workaround), **Layer 3 verified 2026-07-24**: item 572470 renamed via the tool (ack echoed the new name; list read-back confirms text change with [ ] state untouched — the cancellation-workaround shape end-to-end).

## P8 — project discovery & state (approved 2026-07-25)

11. **Project discovery + project state** — friction (operator, 2026-07-25): choosing which project to attach a new task to, and judging what state a project is in, is fully manual today (a hand-maintained selection table in the operator's vault, snapshot-dated). The instance holds 200+ projects, many of them stale goal-tree nodes. Current tool surface, verified live 2026-07-25: `get_projects` pages with no filters; `get_project` renders only id/name and a bare numeric status (`#2`, label unresolved); passing extra `fields` (owner, dates, group) changes nothing visible in the output. Spike first (P2/P6 precedent): what do the project endpoints actually expose — complex filters on `/project/list`, status-label resolution, owner/dates/group fields, and whether "state" (task counts by status, last-activity recency) requires composing `task/list` calls. Tool design after the spike; expected shape: `search_projects` (name substring / active-only discovery) + `get_project_overview` (card with human-readable status + task aggregates + recency signal), read-only, token-bounded. Evidence goes to `docs/spikes/projects.md`.
    **Layer 3 verified 2026-07-26** (reviewing session, read-only live runs via Inspector CLI): search_projects `{nameContains: "MCP", activeOnly: true}` → exactly #572465 MCP-Test with owner rendered; filterless call → English refusal with `isError: true`; overview of 572465 → alive case (3 scanned, By status, recency list sorted desc); overview of 7854 → dead case (Completed label = the operator-confirmed mapping, 74 closed, "looks inactive" verdict); overview of large project 14 → scan cap fires ("300+ scanned … lower bounds" with the search_tasks hint) while recency stays exact (53 tasks, top-10 list); get_project 326895 → widened card closes the original friction (status label + owner + start date where `#2` used to be). Build + 193/193 tests re-run locally by the reviewer.
    **Status (2026-07-26): IMPLEMENTED.** Design approved 2026-07-26 with changes, all applied: (1) status map locked — operator confirmed against the UI that project 7854 displays as Completed, so `{0: Draft, 1: Completed, 2: Active}` (constant `PROJECT_STATUS_LABELS` in `src/format.ts`); (2) the overview's recency count reports `100+` when its single page is full, never a bare 100; (3) English-only server text everywhere (CLAUDE.md update 2026-07-26): full sweep of `src/format.ts` output templates, `src/skills.ts` prompts, and remaining Russian comments/labels — enforced by `tests/english-output.test.ts` (source-wide no-Cyrillic scan + rendered-output smoke tests). Shipped: `search_projects` (filters 5001/5006/5002/5004, ≥1 required), `get_project_overview` (card + status counts scan-capped at 300 with explicit lower bounds + type-79 recency + client-sorted recent tasks), widened `get_project` default fields, `formatProject` renders owner/group/parent/dates/status label. 32 tools total. Layer 1: 193 tests pass. Layer 2: Inspector CLI — discovery, refusal path (dummy creds), live read-only round-trips (search by name+active → MCP-Test; overview of 572465 → alive; overview of 7854 → Completed, 74 closed, inactive).
    **Spike (2026-07-25): DONE.** Key findings: `/project/list` supports complex filters (5001 name-contains, 5006 status — both verified live); project `status` is returned as bare `{id}` with no label anywhere in the API — the three fixed system statuses map to a client-side constant (0 = Draft, 1 = Completed, 2 = Active; 2 proven, 0/1 strongly evidenced); the `#2`-only output was a fetch gap (no label served) AND a render gap (`formatProject` drops all fields beyond id/name/status); state aggregates compose from `task/list` (project filter type 5 + updated-after type 79, `status.isActive` per row, no server-side sort — client-sort one type-79 page). All probes read-only; no artifacts created.

## P9 — task custom fields: read, filter, write (approved 2026-07-26)

12. **Custom-field support on tasks** — friction (operator, 2026-07-26): the RevOps sprint/planning layer lives entirely in task custom fields (22571 `[RevOps] Sprint`, 22453 `Estimation RevOps`, 22451 `Time spent RevOps`, 22443 `Status [RevOps]`, 21752 `Priority`, 22559 `Complexity Level`) and is fully invisible to the MCP today. Reviewing-session findings, verified 2026-07-26 by raw REST probe on production task 412626 (read-only):
    - **Fetch works today**: `GET task/{id}?fields=id,name,22571,22453,…` (numeric field ids in `fields`; the literal word `customFieldData` is silently ignored) returns a rich `customFieldData` array — field name, type, `enumValues` (all 12 sprint values), raw `value` and human-readable `stringValue`.
    - **Render gap**: `formatTask`/`formatSingleTask` drop `customFieldData` — fetched fields never reach the agent (same disease `formatProject` had before P8).
    - **Filter gap (real bug)**: custom-field task filters need a 4th key `field` (custom field id) in the filter object; `get_tasks`'s `filterSchema` is `{type, operator, value}` — Zod silently strips `field`, so custom-field filtering is impossible even via raw `get_tasks`.
    - **Write nuance**: `Estimation RevOps`/`Time spent RevOps` are typeName "Data tag summaries" (type 23) — computed sums over data tag entries, NOT directly writable fields; estimation writes presumably go through an estimation data tag (add_time_entry/datatag-59 precedent). Plain List fields (Sprint, Status [RevOps], Priority, Complexity) should write via `customFieldData` in the task body. `create_task`/`update_task` schemas are fixed whitelists — no custom-field write path exists.
    Scope: (a) render `customFieldData` (name: stringValue rows) + a documented way to request fields by id on get_task/get_task_full/get_tasks; (b) `field` key in the ad-hoc filter schema + custom-field sugar in `search_tasks`; (c) SPIKE first for writes — find the estimation data tag via `list_datatags` and probe an entry write on MCP-Test; probe a List-field write (`customFieldData` in POST task body) on MCP-Test; design write tools only after the spike (P2/P6 precedent). Evidence to `docs/spikes/task-custom-fields.md`.
    **Part A Layer 3 verified 2026-07-26** (reviewing session, read-only live runs via a stdio JSON-RPC probe — Inspector CLI cannot pass nested-object args): reference workflow in one call — `search_tasks {customField: {22571, "Sprint 10 - 2026"}, customFieldIds: [22453,22451,22443], assigneeId: 403}` → task 574458 with `Time spent RevOps: 5 ч 8 мин | Estimation RevOps: 4 ч | [RevOps] Sprint: Sprint 10 - 2026` inline; `get_task 412626` with numeric ids → all four RevOps fields rendered (`30 ч` estimation etc.); nonsense label `"Sprint 77 - 2077"` → zero rows with the relax-filters hint; spike probe task 574545 read back exactly as the spike's artifact inventory states (Estimation 1 ч 30 мин, Sprint 4, Advanced; 21752/22443 absent — consistent with the template-attachment finding). Build + 205/205 tests re-run by the reviewer.
    **Status (2026-07-26): Part A (read+filter) IMPLEMENTED; Part B write spike DONE — write-tool design in `docs/spikes/task-custom-fields.md` awaiting review.** Part A shipped: `customFieldData` renders as `name: stringValue` segments in task rows (DETAILED only; CONCISE stays identifier-grade); `fields` descriptions on get_task/get_tasks/get_task_full document the numeric-id mechanic (get_task_full gained a `fields` param — it had none); ad-hoc `filterSchema` gained the `field` key (the Zod-strips-field bug is fixed and regression-tested); `search_tasks` gained `customField: {fieldId, value}` (List filter type 106, exact label match, verified live) and `customFieldIds` (display-only fetch list). No org field ids hardcoded — they live in parameter examples only. Reference workflow verified live in ONE call: sprint filter + estimation/time-spent rendered per row. Layer 1: 205 tests pass. Part B spike key findings: estimation writes via data tag 61 work ONLY with the `{from,to}` clock shape (`durationSec`-only is a silent no-op); List-field writes validate NOTHING (garbage stored verbatim on attached fields; writes to template-unattached fields — 21752 Priority, 22443 Status [RevOps] on a plain task — return success and store nothing, even the 202 variant); partial customFieldData updates preserve other fields; summary field 22453 updates immediately. Proposed tools: `set_task_custom_field` (enum-validated, read-back-verified) + `add_estimation` (≤24h chunking); raw customFieldData passthrough on update_task rejected.
    **Part B write tools IMPLEMENTED 2026-07-26 (design approved with the workday-layout amendment), Layer 3 verified 2026-07-26** (reviewing session, gated writes on MCP-Test task 574545): validate_only reproduced the review's 10.5h example verbatim (3 entries, 2 visual days); enum refusal on "Sprint 99 - 2099" listed all 12 options with zero writes; real Sprint write → ✓ with read-back; write to template-unattached 21752 Priority → the actionable non-persist error (false-✓ class killed); real add_estimation 1.5h with workday layout → entry 10:00-11:30 (key 127904), read-back new total exactly 25 ч 30 мин + 1 ч 30 мин = 27 ч. Multi-entry chaining not exercised live (single-chunk write; chaining is unit-tested and shares the add_time_entry mechanism verified in P2). Build + 223/223 tests re-run by the reviewer. `src/tools/customfields.ts`: `set_task_custom_field` (safe-mode → field lookup → enum validation → write → mandatory read-back; template-attachment no-ops become actionable errors) + `add_estimation` (APPEND semantics with read-back new total in the ack; optional `workday {from,to,exclusions}` cosmetic layout, NO default day shape in code; plain 00:00-24:00 chunks when omitted; all-or-nothing validation; `validate_only`; partial-failure reporting; from/to shape only). Addendum finding while implementing: `to: "24:00"` is the working 24h chunk — `00:00-00:00` stores durationSec 86400 that the summary IGNORES (third silent-failure variant, entry 127902; working entry 127903 verified +24h on the summary). 34 tools total; Layer 1: 223 tests pass; Layer 2: discovery + validate_only plan (review's 10.5h example exact) + live enum/summary refusals verified with zero writes. Layer 3 items for the reviewing session: real `set_task_custom_field` write on an MCP-Test task; real `add_estimation` write incl. multi-entry chaining; template-bound fields (21752/22443) still pending an operator-created RevOps-template task in MCP-Test.

## P10 — time report: per-person workload & unlogged days (approved 2026-07-26)

13. **Cross-task time reporting** — friction (operator, 2026-07-26): "team workload over a period" and "which days have no time logged" are unanswerable today. `get_task_time_entries` reads entries per TASK only; `search_tasks` + `customFieldIds` shows per-task lifetime sums (22451) with no per-day/per-person breakdown; the Planfix report that covers this is UI-only (`run_report` still in the researched backlog). The needed cut is data tag 59 entries by USER + DATE RANGE, task-agnostic.
    SPIKE first (read-only): (a) does `POST /datatag/59/entry/list` (or another entry-list surface) filter by user and/or date without a taskId — and what does pagination look like at period scale; (b) **scope check: does the operator's user-scope API key return OTHER users' time entries at all** — if not, the tool is personal-only (still valuable for the unlogged-days case); (c) confirm entry fields available for aggregation (user 173, date 175, time-range 185). Evidence to `docs/spikes/time-report.md`.
    Tool design after the spike; expected shape: `get_time_report(userIds, dateFrom, dateTo)` → per-user per-day totals + days with zero entries in the range. Policy stays agent-side (log_workday/exclusions precedent): NO team roster and NO working-day calendar in code — user ids come as a parameter; "unlogged day" = any Mon-Fri with zero entries unless the caller passes an explicit day list, and the output states which definition was used. Token-bounded output (aggregates, not entry dumps).
    **Status (2026-07-26): spike DONE — all three questions answered positively; design proposal in `docs/spikes/time-report.md` awaiting review.** (1) `POST /datatag/59/entry/list` is task-agnostic: `filters` accept user (type 3103, field 173, value `user:N`) and date (type 3101, field 175, `otherRange` inclusive both ends — verified against `equal otherDate`); no per-task iteration needed. (2) Key scope is FULL TEAM — other users' entries (312, and 2023 entries by a third employee) read fine. (3) Fields 173/175/185 all present per entry incl. `durationSec` and the user's display name; measured cost: July 2026 for user 403 = 56 entries = ONE call (aggregated in-probe: 13 days, 109.05 h). Proposed `get_time_report(userIds[≤10], dateFrom, dateTo[≤92 days], workingDays?)` — per-user totals + per-day rows + unlogged days with the definition stated verbatim in output; scan cap 10 pages/user with the N+ convention. All probes read-only; no artifacts.
    **Layer 3 verified 2026-07-27** (reviewing session, read-only live runs via stdio JSON-RPC probe): two-user July report (403 + 312) — user 403 totals match the spike's independent aggregation exactly (109h 3m, 56 entries, 13 days, 10 of 23 Mon-Fri unlogged) and user 312 renders her own 12 logged days + unlogged list (team scope through the real tool surface); workingDays override on a 5-day slice → definition line switches to the caller-provided variant, Saturday-free candidate set of 4, 2026-07-18 correctly reported unlogged; 212-day range → refused naming the 92-day cap. Build + 234/234 tests re-run by the reviewer.
    **IMPLEMENTED 2026-07-27 (design approved as proposed, reviewer re-verified the load-bearing claims independently).** `src/tools/timeentries.ts`: `get_time_report` exactly per the spike design — filters 3103/173 + 3101/175 otherRange per user, fields `key,173,175,185`, 10-page/user scan cap with N+ lower bounds, range cap 92 days, workingDays override validated inside the range, unlogged-days definition stated verbatim, zero-entry users explicit, aggregates only, `{readOnlyHint: true}` without idempotentHint. 35 tools; Layer 1: 234 tests pass (11 new, incl. filter-shape, cap, plural/edge and data-passthrough cases); Layer 2: discovery + range-cap refusal (dummy creds) + live read-only two-user July run whose totals match the spike's in-probe aggregation exactly (user 403: 109h 3m, 56 entries, 13 days; user 312 renders with 12 days and its own unlogged list).

## P11 — full-text search spike: descriptions & comments (approved 2026-07-27)

14. **Text search beyond task names** — friction (operator + Harbor, 2026-07-27, TG): the key gap named by Harbor after building the sibling server — searching tasks by COMMENT and DESCRIPTION text. Our `search_tasks` covers the name only (filter 8). SPIKE ONLY, no tool yet: (a) does any complex task filter match DESCRIPTION text (sweep the filter-type table in docs + probe candidates live, read-only); (b) does ANY REST surface offer full-text search over comments (swagger sweep for search-shaped endpoints; the UI global search's backing endpoint if visible); (c) if the API cannot, survey the implementation space for a LOCAL INDEX (sync tasks+comments → SQLite FTS or similar inside this stdio server: feasibility, staleness model, size estimate for this account) and the cheap no-index alternative (on-the-fly comment grep over a bounded task set, cost-modeled). Deliverable: evidence + a compared options table in `docs/spikes/fulltext-search.md` — a DECISION input for the operator and Harbor (possible repo merge), not a design commitment.
    **Status (2026-07-27): spike DONE — decision document in `docs/spikes/fulltext-search.md`, awaiting the operator+Harbor decision.** Q1: NO description search — filter 8 is name-only (positive control hit / description-only term «пояснюваність» zero), and a live sweep of all 19 undocumented filter-type codes found no hidden text filter. Q2: NO comment text surface — zero search-shaped endpoints in the swagger; ComplexCommentFilter is date (3000) + author (3001) only; Harbor's diagnosis CONFIRMED. Q3 measured: 163,682 tasks visible to the key (exact), 600k–1M comments (bracketed); task-agnostic `/comment/list` exists but refuses bare calls (400) and costs 6–49 s/call by filter width → FULL index build = 10–25 h one-time; scoped index or bounded grep are the practical shapes; incremental deltas cheap and proven (task filter 79 + comment filter 3000 gt). Recommendation: bounded grep first (`search_task_text`, ≤50 tasks, zero state); scoped `node:sqlite` FTS5 index (engines ≥22.5) only if account-wide search becomes real friction. All probes read-only.

## P12 — file attachments: upload from disk & attach to comments/tasks (approved 2026-07-28)

15. **Document attachment chain** — friction (operator, 2026-07-28): attaching a document to a task or comment is fully manual (e.g. the time-log backfill `.docx` was attached by hand). Today no link in the chain exists: `upload_file_from_url` uploads target-less (and is therefore refused entirely in safe mode) and cannot read the local disk (multipart `POST /file/` deliberately unimplemented upstream); `add_comment`/`create_task`/`update_task` accept no files.
    SPIKE first (writes on MCP-Test only): (a) multipart `POST /file/` from a local path — the server runs on the operator's machine, so disk paths are its natural input; verify the request shape and size limits; (b) attach semantics — `files` in the comment-create body and file attachment to the task card: verify shapes live, incl. whether attaching an already-uploaded fileId works and whether one file can attach to several targets; (c) how attached files surface in reads (get_comments/get_task) so the tools can render them. Evidence to `docs/spikes/file-attachments.md`. Tool design after the spike (expected: attach-capable `add_comment`, upload-and-attach composite with a REQUIRED task/comment target; the target-less upload stays refused in safe mode).
    **Status (2026-07-28): spike DONE — all three questions answered positively; design proposal in `docs/spikes/file-attachments.md` awaiting review.** Multipart `POST /file/` (form field `file`) works from disk incl. a 2 MB binary with a Cyrillic filename (ids 1729843/1729845); attach works on all three surfaces — comment-create `files: [{id}]`, task-update `files: [{id}]`, dedicated `POST /file/{id}/attach/task?id=` — and one fileId re-attaches to multiple comments; reads: `GET /task/{id}/files`, comments with `files` in fields, `GET /file/{id}` with presigned-S3 `downloadUrl` (expires; `size` is KB rounded up, verified 55 B → 1). Gotcha: `file/from-url/` rejects some URLs with code 41 "Unsafe url" (planfix.com itself; three external hosts fine) — inherited by the existing `upload_file_from_url`. Proposed: `add_comment` `files` param (local paths and/or fileIds, safe-mode task gate BEFORE any upload, 50 MB tool-side cap) + `attach_file_to_task` composite (localPath|url|fileId → card attach) + `files` rendering in comment/task reads and `downloadUrl` in `get_file`. Probe task 576633 with 5 attached files left in MCP-Test.
    **Layer 3 verified 2026-07-28** (reviewing session, gated writes on MCP-Test task 576633): disk upload with a Cyrillic filename via attach_file_to_task -> file 1729853 attached with name+KB ack; url source (example.com) -> file 1729855 attached; relative-path and two-sources refusals returned the exact English messages with zero writes; add_comment with a MIXED files array (fresh local path + existing fileId 1729845) -> comment 47922727, read-back renders both file segments with KB sizes. Observation: re-uploading the same local file that was already uploaded in the same session returned the SAME fileId (1729853) - Planfix appears to dedupe identical content+name; harmless, noted for awareness. Build + 251/251 tests re-run by the reviewer.
    **IMPLEMENTED 2026-07-28 (design approved as proposed; 50 MB cap + code-41-as-documented confirmed by review).** `planfixUploadFile` (multipart, single attempt, 120 s window, contents never logged) in `src/client.ts`; `uploadLocalFile` guards (absolute path / exists / not-a-directory / 50 MB tool cap, English refusals) + `attach_file_to_task` (exactly one of localPath|url|fileId, task gate BEFORE any upload, ack with name+KB) in `src/tools/files.ts`; `add_comment` `files` param (paths and/or fileIds, ≤10, gate-before-upload, ack lists attached ids) in `src/tools/comments.ts`; read side — `files` in comment fields (get_comments/get_task_full) rendered as `file: name (#id, N KB)`, `get_file` defaults gain `downloadUrl` (expiry noted in description and output); `upload_file_from_url` description now points at the target-bound tools (still refused in safe mode, guard untouched). 36 tools; Layer 1: 251 tests pass (18 new incl. fetch-level multipart shape); Layer 2: discovery + no-network refusal paths (dummy creds) + live read-only rendering against the spike artifacts (downloadUrl on 1729843; file segments on task 576633's comments incl. the Cyrillic 2 MB file). Layer 3 items: real attach_file_to_task and add_comment-with-files writes on MCP-Test.

## P13 — codebase revision: duplication, overlap, consistency (approved 2026-07-28)

16. **Revision pass over the grown codebase** — friction (operator, 2026-07-28): P8–P12 landed fast; before growing further, audit what exists — tool overlap, duplicated helpers, drifted conventions — and consolidate. Two phases, review-gated between them: Phase 1 = AUDIT ONLY (no code changes) → findings report `docs/audit-2026-07.md` with severity + proposed fix per finding; Phase 2 (after review approves a fix list) = apply approved fixes with tests, no behavior changes beyond what the approved list names. Audit axes at minimum: tool-surface overlap/consolidation candidates; helper duplication across `src/tools/*` (asObj shims, date/HHMM converters, read-back patterns, manual paging loops vs `src/paging.ts`); convention drift (refusal shapes, N+/lower-bound phrasing, has_more footers, ack formats, annotation groups, description patterns); safe-mode coverage sweep (every mutating path gated, gate-before-side-effect ordering); test-suite structure; docs debt (CHANGELOG stale at 1.2.0 and still Russian; README absence for the fork).
    **Status (2026-07-28): Phase 1 AUDIT DONE — `docs/audit-2026-07.md` (34 findings across 7 axes) awaiting the reviewing session's approved fix list.** Headlines: ONE critical — E1, `planfixRequest` retries 5xx/timeouts for non-idempotent writes, so the tools' "writes are NEVER retried" promise is false in that window (double-logging risk; proposal: retry-policy split keeping 429/code-22). Safe-mode sweep PASSES (all 14 mutating paths gated, gate-before-side-effect everywhere; create_task fail-closed on omitted projectId). Duplication verified byte-identical (no drift) for the asObj shim ×4 and the HH:MM helper set ×2; ONE real drift — three duration renderers print `2h 0m` vs `2h`. Tool overlap: 1 deprecation (upload_file_from_url ⊂ attach_file_to_task), everything else justified keep-both; skill-my-tasks prompt still predates search_tasks. Docs debt: upstream Russian README (claims 20 tools), version identity frozen at 1.2.0, publish-shaped package.json under the upstream npm scope. Proposed Phase 2 list: Group A behavior-preserving cleanups (E2 upload error-order, util consolidation, dead types.ts, text refreshes, test fixtures), Group B behavior-changing (E1 retry split, duration-renderer unification), Group C surface-changing operator decisions (fork README, package identity/versioning, upload_file_from_url removal timing, CONCISE on search_tasks). Two [verify-live] items (C8 priority values, C9 update_task status-source claim) fold into the next Layer-3 session.

## Researched backlog (NOT friction-validated — promote to a P-item only when real friction is observed)

Source: 2026-07-24 benchmark against peer PM-tool MCP servers (Atlassian Rovo MCP for Jira — 16 tools incl. JQL search, transitions, worklogs, linking, no deletes; ClickUp/Quire community servers). The fork already matches the peer baseline; items below are candidates, not commitments, per the guiding rule.

- **`create_task_from_template`** — RevOps runs 13 `[RevOps] / …` task templates (Priority/SP/Status fields); bare `create_task` bypasses the standard. Needs a spike first: does REST expose task templates?
- **`run_report`** — the "All completed tasks by employee" report is the SP source for the operator's velocity metric; peer precedent exists (popstas planfix server exposes report list/generation). Would automate velocity collection.
- **`update_comment`** — fix a typo in an already-posted comment without the UI; cheap (API key scopes already allow comment update).
- Not carried over from peers (no plausible local use): task linking (Jira), boards/spaces (ClickUp), MCP prompts beyond the existing two.

## Explicitly out of scope

- ~~**Checklists** — not exposed by the Planfix REST API at all (verified empirically 2026-07 and against API docs).~~ **Falsified 2026-07-24 by the P6 spike** (`docs/spikes/checklists.md`): the `/task/{id}/checklist*` endpoints work fully, reads and writes. Checklists moved to P6 above.
- **Endpoint-coverage parity** with the REST API — coverage is not a goal; friction is.
- **Project creation** — API does not support it; projects are created in the UI.
